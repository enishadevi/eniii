steps:
      - name: Use API Key
        shell: bash
        env:
          # Map the GitHub secret to a local env var
          MY_API_KEY: ${{ secrets.API_KEY }}
        run: |
          echo "The API key is being used securely..."
          # Use $MY_API_KEY in your commands/scripts
